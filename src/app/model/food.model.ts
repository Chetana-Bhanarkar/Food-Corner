export class Food{
    id ? : string ; 
    name ? : string ; 
    price  : number  = 1 ; 
    tags ? :  string[] ; 
    favourite ? : boolean;
    rating ? : number ; 
    imageUrl ? : string ; 
    origins ? : string[] ; 
    cookTime ? : string ; 
}

