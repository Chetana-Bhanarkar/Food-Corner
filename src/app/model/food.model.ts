export class Food{
    id ? : string ; 
    name ? : string ; 
    price ? : number ; 
    tags ? :  string[] ; 
    favourite ? : boolean;
    rating ? : number ; 
    imageUrl ? : string ; 
    origins ? : string[] ; 
    cookTime ? : string ; 
}

